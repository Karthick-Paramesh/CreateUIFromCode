
  # Create UI from Code

  This is a code bundle for Create UI from Code. The original project is available at https://www.figma.com/design/Anw95bPGvVQ0Rb9qRT9GBq/Create-UI-from-Code.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  